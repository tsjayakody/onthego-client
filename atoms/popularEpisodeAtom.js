import { atom } from "recoil";

export  const popularEpisodeState = atom({
    key: "popularEpisodeState",
    default:[],
});

export  const popularEpisodeGridState = atom({
    key: "popularEpisodeGridState",
    default:[],
});
